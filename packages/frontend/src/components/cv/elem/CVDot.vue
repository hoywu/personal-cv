<template>
  <Dot color="var(--neutral-500)" class="dot" />
</template>

<style scoped>
.dot {
  display: inline-block;
  width: calc(var(--cv-font-size) / 2);
  height: calc(var(--cv-font-size) / 2);
  margin-right: calc(var(--cv-font-size) / 2);
  padding-bottom: calc(var(--cv-font-size) / 2);
  vertical-align: bottom;
}
</style>
